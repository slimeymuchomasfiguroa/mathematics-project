fn main() {
    // Define variables
    let x = 5;
    let y = 10;

    // Perform some arithmetic operations
    let result = x + y;

    // Print the result
    println!("The sum of {} and {} is {}", x, y, result);
}
